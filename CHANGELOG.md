# Changelog

このファイルでは、利用者に影響する変更を記録します。

## [Unreleased]

### Added

- `HorizontalPicker` / `VerticalPicker` の `Int` / `Float` API に省略可能な `progress: PickerProgress? = null` を追加。選択値とは独立した進捗までの目盛りを明るく表示でき、強調色も指定可能
- 進捗は描画時に読み取り、可視目盛りの描画レイヤーだけを更新。進捗だけの更新では再構成・再レイアウト・ラベル再計算を不要にし、可視目盛りの描画情報をキャッシュ

既存の呼び出しは変更不要です。`progress` を省略すると従来どおり動作します。追加引数によってバイナリのシグネチャは変わるため、アップデート時は利用側を再ビルドしてください。公開済みの `0.1.1` にはこの機能は含まれません。

### Performance / Refactoring

- 可視目盛り用のプリミティブ配列をスクロール中も再利用し、目盛りごとの描画オブジェクトの作り直しを削減
- ラベル対象のインデックスのみを走査し、文字計測キャッシュを最大 128 件に調整。ラベル非表示時の描画処理を早期終了
- 横・縦ラベルの配置計算と共通の回転描画を整理

## [0.1.1] - 2026-07-16

Maven Central での初回公開版です。

### Fixed

- 範囲・step・コールバックを動的に変更したとき、古い状態を参照することがある問題を修正
- `Int` API で `Float` が正確に表現できない値を受け取ってしまう問題を防止
- 小数の開始値、縦向きで回転したラベル、非整数 step 時のアクセシビリティ範囲を修正

### Documentation

- Maven Central からの導入手順、制約、公開 API を README に記載
